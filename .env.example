# Google Cloud / Vertex AI Configuration
VERTEX_AI_PROJECT=your-gcp-project-id
VERTEX_AI_LOCATION=us-central1

# Path to GCP service account key file
# This file should have Vertex AI permissions
GOOGLE_APPLICATION_CREDENTIALS_PATH=./credentials

# Gemini Model Configuration
GEMINI_MODEL=gemini-1.5-pro
GEMINI_TEMPERATURE=0.7
GEMINI_MAX_OUTPUT_TOKENS=8192

# Agent Service URLs (for docker-compose, these are auto-configured)
RESEARCH_AGENT_URL=http://localhost:3001
ANALYSIS_AGENT_URL=http://localhost:3002
WRITER_AGENT_URL=http://localhost:3003
MCP_WEB_SEARCH_URL=http://localhost:3100/sse

# Timeouts
AGENT_TIMEOUT=30000

# Logging
LOG_LEVEL=info
NODE_ENV=development

# Web Search Configuration (Optional)
# By default, uses DuckDuckGo (no API key needed)
# For better results, get a free Brave Search API key: https://brave.com/search/api/
# BRAVE_SEARCH_API_KEY=your-brave-api-key-here
